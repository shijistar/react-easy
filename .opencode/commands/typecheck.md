---
description: Run TypeScript check
agent: build
---

Run `npx tsc -p tsconfig.json --noEmit` for this repository and summarize type errors briefly.

This project uses strict TypeScript settings, so focus on actionable fixes and affected exported APIs.
