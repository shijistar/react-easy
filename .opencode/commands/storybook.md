---
description: Start Storybook dev server
agent: build
---

Start the Storybook development server for this component library with `npm run storybook`.

Report the local URL, startup status, and any errors that would block component preview or docs usage.
