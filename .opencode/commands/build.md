---
description: Build library outputs
agent: build
---

Build this React component library from source. Run `npm run build`, which should produce `lib/` and `es/` from `src/` and then run the sourcemap correction step.

Treat `lib/` and `es/` as generated artifacts, summarize any failures, and mention whether generated outputs changed.
