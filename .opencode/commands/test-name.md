---
description: Run one Jest case
agent: build
---

Run a single Jest test file or test name for this repository.

Use `npx jest --runTestsByPath $ARGUMENTS` when given a file path, and if a case name is also provided use `-t` for that specific test.

Summarize only the important pass/fail details.
