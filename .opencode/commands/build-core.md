---
description: Run Father build only
agent: build
---

Run `npm run build-core` for this repository. This should use Father to build the library from `src/` without the follow-up sourcemap correction step.

Summarize failures briefly and mention whether a full `npm run build` is still recommended.
