Renders a configurable context menu with support for shortcuts, separators, and nested submenu items.

## When to use

You need a right-click (or other trigger) menu on an element, with keyboard shortcuts, separators and multi-level submenus.
